<?php
/**
 * @var Bold_CheckoutPaymentBooster_Block_Payment_Form_Base $this
 */
?>
<!-- Bold CheckoutPaymentBooster build: <?php echo $this->escapeHtml($this->getFrontendBuildInfo()['buildId']) ?> -->

<script type="text/javascript">
    if (typeof window.bold === 'undefined') {
        window.bold = {};
    }

    window.bold.checkoutPaymentBoosterBuild = <?php echo json_encode($this->getFrontendBuildInfo()) ?>;
    window.bold.version = window.bold.checkoutPaymentBoosterBuild.buildId;
    window.bold.moduleVersion = window.bold.checkoutPaymentBoosterBuild.moduleVersion;

    window.bold.walletCheckoutConfig = <?php echo json_encode($this->getWalletCheckoutJsConfig()) ?>;

    /**
     * Bold Payments base class.
     */
    const BoldBase = Class.create({
        boldPaymentsInstance: null,
        authorizedEmail: null,
        fastlaneInstance: null,
        fastlaneProfileData: null,
        gatewayData: null,
        buildFastlaneInstanceInProgress: false,
        buildPaymentsInstanceInProgress: false,
        syncCheckoutSessionPromise: null,
        cachedCartData: null,
        getCartDataPromise: null,
        walletCheckoutCreateAllowed: false,
        walletCheckoutSessionActive: false,
        walletCreateOrderConsumed: false,
        ppcpCheckoutClicked: false,
        walletPayCreateInFlight: false,
        walletPayCreatePromise: null,
        createExpressOrderInFlight: false,
        createExpressOrderPromise: null,
        isAvailable: <?php echo $this->isAvailable(); ?>,
        groupLabel: '<?php echo $this->getGroupLabel() ?>',
        boldApiUrl: '<?php echo $this->getBoldApiUrl(); ?>',
        jwtToken: '<?php echo $this->getJwtToken(); ?>',
        epsAuthToken: '<?php echo $this->getEpsAuthToken(); ?>',
        epsUrl: '<?php echo $this->getEpsUrl(); ?>',
        epsStaticUrl: '<?php echo $this->getEpsStaticUrl(); ?>',
        epsGatewayId: '<?php echo $this->getEpsGatewayId(); ?>',
        quoteCurrencyCode: '<?php echo $this->getQuoteCurrencyCode(); ?>',
        isFastlaneAvailable: <?php echo $this->isFastlaneAvailable(); ?>,
        billingAddressFieldsMap: {
            'firstname': 'billing:firstname',
            'lastname': 'billing:lastname',
            'company': 'billing:company',
            'telephone': 'billing:telephone',
            'street1': 'billing:street1',
            'street2': 'billing:street2',
            'city': 'billing:city',
            'region_id': 'billing:region_id',
            'country_id': 'billing:country_id',
            'postcode': 'billing:postcode',
            'use_for_shipping': 'billing:use_for_shipping_yes'
        },
        /**
         * Initialize class.
         *
         * @returns {*|boolean}
         */
        initialize: async function () {
            if (!this.isAvailable) {
                return false;
            }
            if (!this.isFastlaneAvailable) {
                return;
            }
            this.renderFastlaneWatermark();
            await this.authorizeFastlaneUser();
            this.subscribeToEmailChanges();
        },
        /**
         * Observe email changes to authorize Fastlane user.
         *
         * @return {void}
         */
        subscribeToEmailChanges: function () {
            const emailField = this.getEmailField();
            if (!emailField) {
                return;
            }
            emailField.observe('change', () => {
                setTimeout(async () => {
                    await this.authorizeFastlaneUser();
                }, 500);
            });
        },
        /**
         * Remove Fastlane watermark placeholder in case fastlane is not available.
         *
         * @return {void}
         */
        removeWatermarkPlaceholder: function () {
            const watermarkContainer = document.getElementById('fastlane-watermark-container');
            if (watermarkContainer) {
                watermarkContainer.remove();
            }
        },
        /**
         * Render Fastlane watermark.
         *
         * @return {void}
         */
        renderFastlaneWatermark: async function () {
            this.renderFastlaneWatermarkPlaceholder();
            const fastlaneInstance = await this.getFastlaneInstance();
            const watermarkComponent = await fastlaneInstance.FastlaneWatermarkComponent({includeAdditionalInfo: true});
            let watermarkContainer = document.getElementById('fastlane-email-watermark-container');
            if (watermarkContainer) {
                watermarkContainer.style = 'float: right';
                watermarkComponent.render('#fastlane-email-watermark-container');
            }
        },
        /**
         * Render Fastlane watermark placeholder to show watermark immediately before fastlane is initialize.
         *
         * @return {void}
         */
        renderFastlaneWatermarkPlaceholder: function () {
            const emailField = this.getEmailField();
            if (!emailField) {
                return;
            }
            const watermarkContainerId = 'fastlane-watermark-container';
            const existingWatermarkContainer = document.getElementById(watermarkContainerId);
            if (existingWatermarkContainer) {
                return;
            }
            const watermarkContainer = document.createElement('div');
            const watermarkSubContainer = document.createElement('div');
            const watermark = document.createElement('img');
            watermarkSubContainer.id = 'fastlane-email-watermark-container';
            watermarkSubContainer.style = 'float: right; margin-top: 5px';
            watermark.src = 'https://www.paypalobjects.com/connect-boba/assets/FastLaneLogoSmall.svg';
            watermark.alt = '';
            watermarkSubContainer.appendChild(watermark);
            watermarkContainer.id = watermarkContainerId;
            watermarkContainer.style = '<?php echo $this->getWatermarkContainerStyle(); ?>';
            watermarkContainer.appendChild(watermarkSubContainer);
            emailField.insertAdjacentElement('afterend', watermarkContainer);
        },
        /**
         * Build Fastlane Instance considering gateway type.
         *
         * @return {Promise<{}>}
         */
        getFastlaneInstance: async function () {
            if (this.fastlaneInstance) {
                this.buildFastlaneInstanceInProgress = false;
                return this.fastlaneInstance;
            }
            if (this.buildFastlaneInstanceInProgress) {
                return new Promise((resolve) => {
                    setTimeout(() => {
                        resolve(this.getFastlaneInstance());
                    }, 500);
                });
            }
            this.buildFastlaneInstanceInProgress = true;
            const boldPaymentsInstance = await this.getBoldPaymentsInstance();
            const fastlaneClient = await boldPaymentsInstance.getFastlaneClientInit({
                shouldLoadScript: true
            });
            this.gatewayData = fastlaneClient[this.epsGatewayId] || null;
            if (!this.gatewayData) {
                return null;
            }
            if (this.gatewayData.is_test_mode) {
                window.localStorage.setItem('axoEnv', 'sandbox');
                window.localStorage.setItem('fastlaneEnv', 'sandbox');
            }
            if (this.gatewayData.type === 'braintree') {
                this.fastlaneInstance = await this.buildBraintreeFastlaneInstance();
            }
            if (this.gatewayData.type === 'ppcp') {
                this.fastlaneInstance = await this.buildPpcpFastlaneInstance();
            }
            this.buildFastlaneInstanceInProgress = false;
            return this.fastlaneInstance;
        },
        /**
         * Build Fastlane Instance for the Braintree.
         *
         * @return {Promise<void>}
         */
        buildBraintreeFastlaneInstance: async function () {
            await this.loadScript(
                'https://js.braintreegateway.com/web/3.106.0/js/client.min.js'
            );
            await this.loadScript(
                'https://js.braintreegateway.com/web/3.106.0/js/data-collector.min.js'
            );
            await this.loadScript(
                'https://js.braintreegateway.com/web/3.106.0/js/hosted-fields.min.js'
            );
            await this.loadScript(
                'https://js.braintreegateway.com/web/3.106.0/js/fastlane.min.js'
            );
            const clientInstance = await window.braintree.client.create({
                authorization: this.gatewayData.client_token,
            });
            const dataCollectorInstance = await window.braintree.dataCollector.create(
                {
                    client: clientInstance,
                }
            );
            const {deviceData} = dataCollectorInstance;
            return window.braintree.fastlane.create({
                authorization: this.gatewayData.client_token,
                client: clientInstance,
                deviceData: deviceData,
            });
        },
        /**
         * Build Fastlane Instance for the PPCP.
         *
         * @return {Promise<{}>}
         * @private
         */
        buildPpcpFastlaneInstance: async function () {
            return window.paypal.Fastlane();
        },
        /**
         * Authorize user with email.
         *
         * @return {Promise<void>}
         */
        authorizeFastlaneUser: async function () {
            const emailField = this.getEmailField();
            if (!emailField || !emailField.value || this.authorizedEmail === emailField.value) {
                return;
            }
            if (!window.Validation.validate(emailField)) {
                return;
            }
            checkout.setLoadWaiting('billing', true);
            this.authorizedEmail = emailField.value;
            try {
                const {identity} = await this.getFastlaneInstance();
                const {customerContextId} = await identity.lookupCustomerByEmail(emailField.value);
                checkout.setLoadWaiting(false);
                if (!customerContextId) {
                    this.removeAddressButton();
                    this.fastlaneProfileData = null;
                    if (window.bold.fastlanePaymentMethod) {
                        window.bold.fastlanePaymentMethod.renderFastlanePaymentComponent();
                    }
                    return;
                }
                const {
                    authenticationState,
                    profileData
                } = await identity.triggerAuthenticationFlow(customerContextId);
                if (authenticationState === 'succeeded') {
                    checkout.setLoadWaiting('billing', true);
                    this.fastlaneProfileData = profileData;
                    if (profileData.shippingAddress) {
                        this.updateBillingAddressFormValues(profileData.shippingAddress);
                        this.addAddressButton();
                    }
                    if (window.bold.fastlanePaymentMethod) {
                        window.bold.fastlanePaymentMethod.selectPaymentMethod();
                        window.bold.fastlanePaymentMethod.renderFastlanePaymentComponent();
                    }
                    checkout.setLoadWaiting(false);
                }
            } catch (error) {
                console.error('Error:', error);
            }
        },
        /**
         * Fill billing address form with Fastlane address.
         *
         * @param {{}} fastlaneAddress
         * @return {void}
         */
        updateBillingAddressFormValues: function (fastlaneAddress) {
            const magentoAddress = this.convertFastlaneAddressToMagentoAddress(fastlaneAddress);
            Object.keys(this.billingAddressFieldsMap).forEach((field) => {
                if (magentoAddress[field] && document.getElementById(this.billingAddressFieldsMap[field])) {
                    const element = document.getElementById(this.billingAddressFieldsMap[field]);
                    if (!element) {
                        return;
                    }
                    element.value = magentoAddress[field];
                }
            });
        },
        /**
         * Convert Fastlane address to Magento address.
         *
         * @param fastlaneAddress {{}}
         * @return {{}}
         */
        convertFastlaneAddressToMagentoAddress: function (fastlaneAddress) {
            let magentoAddressRegionId = null;
            const fastlaneCountryCode = fastlaneAddress.countryCodeAlpha2 || fastlaneAddress.address?.countryCode;
            const fastlaneRegionCode = fastlaneAddress.region || fastlaneAddress.address?.adminArea1;
            if (window.billingRegionUpdater.regions[fastlaneCountryCode]) {
                Object.entries(window.billingRegionUpdater.regions[fastlaneCountryCode]).each((region) => {
                    const [regionId, regionData] = region;
                    if (regionData.code === fastlaneRegionCode) {
                        magentoAddressRegionId = regionId;
                    }
                })
            }
            const telephone = typeof fastlaneAddress.phoneNumber === 'string'
                ? fastlaneAddress.phoneNumber
                : fastlaneAddress.phoneNumber?.countryCode + fastlaneAddress.phoneNumber?.nationalNumber;
            return {
                'firstname': fastlaneAddress.firstName || fastlaneAddress.name?.firstName,
                'lastname': fastlaneAddress.lastName || fastlaneAddress.name?.lastName,
                'company': fastlaneAddress.company,
                'telephone': telephone,
                'street1': fastlaneAddress.streetAddress || fastlaneAddress.address?.addressLine1,
                'street2': fastlaneAddress.extendedAddress || fastlaneAddress.address?.addressLine2,
                'city': fastlaneAddress.locality || fastlaneAddress.address?.adminArea2,
                'region_id': magentoAddressRegionId,
                'postcode': fastlaneAddress.postalCode || fastlaneAddress.address?.postalCode,
                'country_id': fastlaneCountryCode,
            };
        },
        /**
         * Remove change address button from billing address form.
         *
         * @return {void}
         */
        removeAddressButton: function () {
            const fastlaneAddressContainer = document.getElementById('fastlane-address-container');
            if (fastlaneAddressContainer) {
                fastlaneAddressContainer.remove();
            }
        },
        /**
         * Add change address button on billing address form.
         *
         * @return {{}}
         */
        addAddressButton: function () {
            this.removeAddressButton();
            const fastlaneAddressContainer = document.createElement('fieldset');
            fastlaneAddressContainer.id = 'fastlane-address-container';
            fastlaneAddressContainer.style = '<?php echo $this->getAddressContainerStyle(); ?>';
            const updateAddressButton = document.createElement('button');
            updateAddressButton.type = 'button';
            updateAddressButton.className = 'button';
            updateAddressButton.innerHTML = 'Change Address';
            updateAddressButton.addEventListener('click', () => {
                this.getFastlaneInstance().then((fastlaneInstance) => {
                    if (!fastlaneInstance) {
                        return;
                    }
                    fastlaneInstance.profile.showShippingAddressSelector().then((editAddressResult) => {
                        if (!editAddressResult.selectionChanged) {
                            return;
                        }
                        this.updateBillingAddressFormValues(editAddressResult.selectedAddress);
                    });
                });
            });
            fastlaneAddressContainer.appendChild(updateAddressButton);
            const billingAddressForm = document.getElementById('billing-new-address-form');
            if (billingAddressForm) {
                billingAddressForm.appendChild(fastlaneAddressContainer);
            }
        },
        /**
         * Sync Bold checkout session tokens from Magento before Payments SDK init.
         *
         * @returns {Promise<boolean>}
         */
        syncCheckoutSessionFromServer: function () {
            if (this.syncCheckoutSessionPromise) {
                return this.syncCheckoutSessionPromise;
            }
            const cfg = window.bold.walletCheckoutConfig;
            if (!cfg || !cfg.getCheckoutSessionUrl) {
                return Promise.resolve(false);
            }
            this.syncCheckoutSessionPromise = new Promise((resolve, reject) => {
                new Ajax.Request(cfg.getCheckoutSessionUrl, {
                    method: 'post',
                    parameters: {form_key: cfg.formKey},
                    requestHeaders: {Accept: 'application/json'},
                    onSuccess: (response) => {
                        try {
                            resolve(this.applyCheckoutSessionPayload(JSON.parse(response.responseText)));
                        } catch (e) {
                            reject(e);
                        }
                    },
                    onFailure: () => {
                        reject(new Error('Failed to sync Bold checkout session'));
                    },
                    onComplete: () => {
                        this.syncCheckoutSessionPromise = null;
                    }
                });
            });
            return this.syncCheckoutSessionPromise;
        },
        /**
         * Apply server checkout session payload; reset Payments SDK when public_order_id rotates.
         *
         * @param {{}} payload
         * @returns {boolean}
         */
        applyCheckoutSessionPayload: function (payload) {
            if (!payload || !payload.public_order_id) {
                return false;
            }
            const previousId = this.publicOrderId;
            const rotated = !!(previousId && previousId !== payload.public_order_id);
            this.publicOrderId = payload.public_order_id;
            window.bold.publicOrderId = payload.public_order_id;
            if (payload.jwt_token) {
                this.jwtToken = payload.jwt_token;
            }
            if (payload.eps_auth_token) {
                this.epsAuthToken = payload.eps_auth_token;
            }
            if (payload.bold_api_url) {
                this.boldApiUrl = payload.bold_api_url;
            }
            if (payload.eps_gateway_id) {
                this.epsGatewayId = payload.eps_gateway_id;
            }
            if (rotated) {
                console.warn('[Bold] public_order_id rotated', previousId, '->', payload.public_order_id);
                this.resetBoldPaymentsInstance();
                if (window.bold.boldPaymentMethod) {
                    window.bold.boldPaymentMethod.walletButtonsRendered = false;
                }
            }
            return rotated;
        },
        applyBoldCheckoutSession: function (payload) {
            return this.applyCheckoutSessionPayload(payload);
        },
        resetBoldPaymentsInstance: function () {
            this.boldPaymentsInstance = null;
            this.buildPaymentsInstanceInProgress = false;
        },
        /**
         * Load SPI SDK.
         *
         * @returns {Promise<void>}
         */
        getBoldPaymentsInstance: async function () {
            try {
                await this.syncCheckoutSessionFromServer();
            } catch (e) {
                console.warn('[Bold] Checkout session sync failed before Payments SDK init', e);
            }
            if (this.boldPaymentsInstance) {
                this.buildPaymentsInstanceInProgress = false;
                return this.boldPaymentsInstance;
            }
            if (this.buildPaymentsInstanceInProgress) {
                return new Promise((resolve) => {
                    setTimeout(() => {
                        resolve(this.getBoldPaymentsInstance());
                    }, 500);
                });
            }
            this.buildPaymentsInstanceInProgress = true;
            await this.loadScript(this.epsStaticUrl + '/js/payments_sdk.js');
            const initialData = {
                'eps_url': this.epsUrl,
                'eps_bucket_url': this.epsStaticUrl,
                'group_label': this.groupLabel,
                'trace_id': '<?php echo $this->getPublicOrderID(); ?>',
                'payment_gateways': [
                    {
                        'gateway_id': Number(this.epsGatewayId),
                        'auth_token': this.epsAuthToken,
                        'currency': this.quoteCurrencyCode,
                    }
                ],
                'callbacks': {
                    /* Sync body for Apple Pay gesture; SDK still expects a Promise return value. */
                    'onClickPaymentOrder': function (paymentType, paymentPayload) {
                        const isPayPalButton = paymentType === 'ppcp';
                        const isWalletButton = this.isWalletCheckoutPayment(paymentPayload)
                            || paymentType === 'apple'
                            || paymentType === 'google';
                        if (!isPayPalButton && !isWalletButton) {
                            return Promise.resolve();
                        }
                        if (isPayPalButton) {
                            this.ppcpCheckoutClicked = true;
                        }
                        this.armWalletCheckoutCreate();
                        if (isWalletButton) {
                            this.walletCheckoutSessionActive = true;
                        }
                        if (!this.getCachedCartData()) {
                            this.prefetchCartData().catch(function (e) {
                                console.warn('[Bold] Wallet cart prefetch on click failed', e);
                            });
                        }
                        return Promise.resolve();
                    }.bind(this),
                    'onCreatePaymentOrder': async function (paymentType, paymentPayload) {
                        try {
                            if (this.isWalletCheckoutPayment(paymentPayload)
                                || paymentType === 'apple'
                                || paymentType === 'google') {
                                if (this.shouldDeferWalletButtonCreate()) {
                                    return undefined;
                                }
                                window.bold.walletPayOrderLock = true;
                                this.walletCreateOrderConsumed = true;
                                this.walletCheckoutCreateAllowed = false;
                                this.walletCheckoutSessionActive = true;
                                return await this.handleWalletCreatePaymentOrder(paymentPayload);
                            }
                            if (paymentType !== 'ppcp') {
                                return;
                            }
                            if (this.shouldBlockPpcpMountCreate()) {
                                return undefined;
                            }
                            const paymentMethod = window.bold.boldPaymentMethod;
                            const isCardTokenize = !!(paymentMethod && paymentMethod.isTokenizing);
                            if (!isCardTokenize) {
                                if (!this.ppcpCheckoutClicked && !this.walletCheckoutCreateAllowed) {
                                    this.ppcpCheckoutClicked = true;
                                    this.armWalletCheckoutCreate();
                                }
                            }
                            if (!this.shouldAllowPpcpCreatePaymentOrder()) {
                                return undefined;
                            }
                            if (!isCardTokenize) {
                                window.bold.walletPayOrderLock = true;
                                this.walletCreateOrderConsumed = true;
                                this.walletCheckoutCreateAllowed = false;
                            }
                            try {
                                const createOrderResult = await this.createExpressOrder(paymentPayload);
                                return {
                                    payment_data: {
                                        id: createOrderResult.order_id,
                                        order_id: createOrderResult.order_id
                                    }
                                };
                            } finally {
                                if (!isCardTokenize) {
                                    this.ppcpCheckoutClicked = false;
                                }
                            }
                        } catch (e) {
                            const paymentMethod = window.bold.boldPaymentMethod;
                            const wasPayPalAttempt = paymentType === 'ppcp'
                                && !(paymentMethod && paymentMethod.isTokenizing);
                            if (wasPayPalAttempt) {
                                this.handleWalletCheckoutCancelled(paymentType, paymentPayload);
                            }
                            console.error('Failed to create payment order', e);
                            throw e;
                        }
                    }.bind(this),
                    'onUpdatePaymentOrder': async function (paymentType, paymentPayload) {
                        if (this.isWalletExpressPayFlow(paymentType, paymentPayload)) {
                            return await this.handleWalletUpdatePaymentOrder(paymentPayload);
                        }
                    }.bind(this),
                    'onApprovePaymentOrder': async function (paymentType, paymentInformation, paymentPayload) {
                        try {
                            if (paymentType === 'google' || paymentType === 'apple'
                                || this.isWalletCheckoutPayment(paymentPayload)) {
                                await this.handleWalletApprovePaymentOrder(paymentPayload, paymentInformation);
                                return;
                            }
                            if (paymentType !== 'ppcp') {
                                return;
                            }
                            const paymentMethod = window.bold.boldPaymentMethod;
                            const isCardTokenize = !!(paymentMethod && paymentMethod.isTokenizing);
                            if (!isCardTokenize) {
                                await this.handleWalletApprovePaymentOrder(paymentPayload, paymentInformation);
                                return;
                            }
                            if (!paymentInformation.payment_id) {
                                console.error('No payment ID provided from PayPal.');
                                return;
                            }
                            if (paymentMethod) {
                                paymentMethod.paymentId = paymentInformation.payment_id;
                            }
                            if (typeof payment !== 'undefined' && payment.save) {
                                payment.save();
                            }
                        } catch (e) {
                            console.error('Failed to approve payment order', e);
                            throw e;
                        }
                    }.bind(this),
                    'onScaPaymentOrder': async function (type, payload) {
                        if (type !== 'ppcp') {
                            throw new Error('Unsupported payment type');
                        }
                        const url = `${this.boldApiUrl}payments/on_sca`;
                        const orderId = payload.order_id;
                        const scaResult = await fetch(url, {
                            method: 'POST',
                            headers: {
                                'Content-Type': 'application/json',
                                'Authorization': `Bearer ${this.jwtToken}`
                            },
                            body: JSON.stringify(
                                {
                                    'order_id': orderId,
                                    'public_order_id': '<?php echo $this->getPublicOrderID(); ?>',
                                    'gateway_type': 'ppcp',
                                }
                            ),
                        });
                        if (!scaResult.ok) {
                            throw new Error('SCA failed');
                        }
                        const data = await scaResult.json();
                        return {card: data};
                    }.bind(this),
                    'onRequireOrderData': function (requirements) {
                        try {
                            if (!this.getCachedCartData()) {
                                this.prefetchCartData().catch(function (prefetchError) {
                                    console.warn('[Bold] Cart prefetch in onRequireOrderData failed', prefetchError);
                                });
                            }
                            return this.onRequireOrderDataCallback(requirements);
                        } catch (e) {
                            if (typeof checkout !== 'undefined') {
                                checkout.setLoadWaiting('billing', false);
                            }
                            throw e;
                        }
                    }.bind(this),
                    'onErrorPaymentOrder': function (paymentType, paymentPayload) {
                        this.handleWalletCheckoutCancelled(paymentType, paymentPayload);
                    }.bind(this),
                }
            };
            this.boldPaymentsInstance = new window.bold.Payments(initialData);
            this.buildPaymentsInstanceInProgress = false;
            return this.boldPaymentsInstance;
        },
        /**
         * Reset cached Payments SDK so SPI can render again after DOM teardown.
         *
         * @return {void}
         */
        resetPaymentsInstance: function () {
            this.clearWalletCheckoutSession();
            this.createExpressOrderInFlight = false;
            this.createExpressOrderPromise = null;
            this.walletPayCreateInFlight = false;
            this.walletPayCreatePromise = null;
            this.boldPaymentsInstance = null;
            this.buildPaymentsInstanceInProgress = false;
        },
        armWalletCheckoutCreate: function () {
            this.walletCheckoutCreateAllowed = true;
            this.walletCreateOrderConsumed = false;
            window.bold.walletPayOrderLock = false;
        },
        shouldDeferWalletButtonCreate: function () {
            return !!(this.shouldBlockPpcpMountCreate()
                || window.bold.walletPayOrderLock
                || !this.walletCheckoutCreateAllowed
                || this.walletCreateOrderConsumed);
        },
        shouldBlockPpcpMountCreate: function () {
            return !!(window.bold.checkoutSdkMounting
                || window.bold.walletSdkMounting
                || (window.bold.walletPayMountCooldownUntil
                    && Date.now() < window.bold.walletPayMountCooldownUntil));
        },
        shouldAllowPpcpCreatePaymentOrder: function () {
            if (this.shouldBlockPpcpMountCreate()) {
                return false;
            }
            const method = window.bold.boldPaymentMethod;
            if (method && method.isTokenizing) {
                return true;
            }
            if (this.ppcpCheckoutClicked) {
                return !this.walletCreateOrderConsumed;
            }
            if (this.walletCheckoutCreateAllowed && !this.walletCreateOrderConsumed) {
                return true;
            }
            return false;
        },
        isWalletCheckoutSessionBusy: function () {
            return !!(this.walletCheckoutSessionActive
                || this.walletPayCreateInFlight
                || window.bold.walletPayOrderLock);
        },
        shouldBlockWalletDomTeardown: function () {
            return this.isWalletCheckoutSessionBusy() || !!window.bold.walletSdkMounting;
        },
        queuePaymentSectionSyncAfterWallet: function (options) {
            options = options || {};
            if (!window.bold.pendingPaymentSectionSyncAfterWallet) {
                window.bold.pendingPaymentSectionSyncAfterWallet = {
                    hardReload: false,
                    forceRender: false,
                    refreshWallets: false
                };
            }
            const pending = window.bold.pendingPaymentSectionSyncAfterWallet;
            if (options.hardReload) {
                pending.hardReload = true;
                pending.forceRender = true;
            }
            if (options.forceRender) {
                pending.forceRender = true;
            }
            if (options.refreshWallets) {
                pending.refreshWallets = true;
                pending.forceRender = true;
            }
        },
        flushPaymentSectionSyncAfterWallet: function () {
            if (!window.bold.pendingPaymentSectionSyncAfterWallet) {
                return;
            }
            const queued = window.bold.pendingPaymentSectionSyncAfterWallet;
            window.bold.pendingPaymentSectionSyncAfterWallet = null;
            window.bold.scheduleBoldPaymentSectionSync(queued);
        },
        clearWalletCheckoutSession: function () {
            this.walletCheckoutCreateAllowed = false;
            this.walletCheckoutSessionActive = false;
            this.walletCreateOrderConsumed = false;
            this.ppcpCheckoutClicked = false;
            window.bold.walletPayOrderLock = false;
            this.flushPaymentSectionSyncAfterWallet();
        },
        resetWalletSessionForCheckoutRefresh: function () {
            if (this.walletPayCreateInFlight) {
                return;
            }
            this.walletCheckoutSessionActive = false;
            window.bold.walletPayOrderLock = false;
            this.walletCheckoutCreateAllowed = false;
            this.walletCreateOrderConsumed = false;
            this.ppcpCheckoutClicked = false;
        },
        handleWalletCheckoutCancelled: function (paymentType, paymentPayload) {
            this.clearWalletCheckoutSession();
            const method = window.bold.boldPaymentMethod;
            if (method && method.isCurrentRuntime()) {
                method.paymentId = null;
                method.isTokenizing = false;
                method.isCompletingPayment = false;
                if (typeof checkout !== 'undefined') {
                    checkout.setLoadWaiting(false);
                }
            }
            console.warn('[Bold] Wallet checkout cancelled or failed', paymentType || '', paymentPayload || '');
        },
        /**
         * Load specified script with attributes.
         *
         * @param {string} src
         * @param {Object} attributes
         * @returns {Promise<void>}
         * @private
         */
        loadScript: async function (src, attributes = {}) {
            return new Promise((resolve, reject) => {
                const script = document.createElement('script');
                script.src = src;
                script.async = true;
                script.onload = resolve;
                script.onerror = reject;
                if (attributes.constructor === Object) {
                    Object.keys(attributes).forEach((key) => {
                        script.setAttribute(key, attributes[key]);
                    });
                }
                document.head.appendChild(script);
            });
        },
        /**
         * Get email field.
         *
         * @return {HTMLElement}
         * @private
         */
        getEmailField: function () {
            return document.getElementById('billing:email');
        },
        cacheCartData: function (cartData) {
            this.cachedCartData = cartData;
            return cartData;
        },
        getCachedCartData: function () {
            return this.cachedCartData;
        },
        prefetchCartData: function () {
            return this.getCartData();
        },
        invalidateCartDataCache: function () {
            this.cachedCartData = null;
            this.getCartDataPromise = null;
        },
        refreshCartData: function () {
            this.invalidateCartDataCache();
            return this.getCartData();
        },
        getCartData: function () {
            if (this.getCartDataPromise) {
                return this.getCartDataPromise;
            }
            this.getCartDataPromise = new Promise((resolve, reject) => {
                new Ajax.Request('/checkoutpaymentbooster/index/getCartData', {
                    method: 'get',
                    onSuccess: (response) => {
                        try {
                            resolve(this.cacheCartData(JSON.parse(response.responseText)));
                        } catch (e) {
                            reject(e);
                        }
                    },
                    onFailure: () => {
                        reject(new Error('Failed to get cart data'));
                    },
                    onComplete: () => {
                        this.getCartDataPromise = null;
                    }
                });
            });
            return this.getCartDataPromise;
        },
        resolveBillingAddressForCustomerRequirement: function (cartData) {
            if (typeof cartData.billingAddress === 'function') {
                return cartData.billingAddress();
            }
            return cartData.billing_address || {};
        },
        normalizeWalletAmountCents: function (value) {
            const amount = Number(value);
            if (!isFinite(amount) || isNaN(amount)) {
                return 0;
            }
            return Math.round(amount);
        },
        parsePriceTextToCents: function (text) {
            if (!text) {
                return 0;
            }
            const cleaned = String(text).replace(/[^\d.-]/g, '');
            const dollars = parseFloat(cleaned);
            if (!isFinite(dollars) || isNaN(dollars)) {
                return 0;
            }
            return Math.round(dollars * 100);
        },
        readCheckoutGrandTotalCents: function () {
            const selectors = [
                '#checkout-review-table .grand-total .price',
                '.checkout-review-table .grand-total .price',
                '#review-total-table .grand-total .price',
                '.firecheckout-summary .grand-total .price',
                '.grand-total .price'
            ];
            for (let i = 0; i < selectors.length; i++) {
                const el = document.querySelector(selectors[i]);
                if (el) {
                    const cents = this.parsePriceTextToCents(el.innerText || el.textContent);
                    if (cents > 0) {
                        return cents;
                    }
                }
            }
            return 0;
        },
        normalizeOrderDataRequirements: function (requirements) {
            if (Array.isArray(requirements)) {
                return requirements;
            }
            if (requirements && Array.isArray(requirements.requirements)) {
                return requirements.requirements;
            }
            if (requirements && typeof requirements === 'object') {
                return Object.keys(requirements);
            }
            return ['totals', 'items', 'customer', 'shipping_options', 'shipping_address'];
        },
        ensureCartDataForOrderData: function (cartData) {
            const fallback = this.buildMinimalCartDataFallback();
            const snapshot = Object.assign({}, fallback, cartData || {});
            if (!snapshot.totals || typeof snapshot.totals !== 'object') {
                snapshot.totals = fallback.totals;
            }
            if (!snapshot.cart_items || !snapshot.cart_items.length) {
                const totals = snapshot.totals || {};
                const amount = this.normalizeWalletAmountCents(totals.order_total)
                    || this.readCheckoutGrandTotalCents();
                snapshot.cart_items = [{price: amount, title: 'Order'}];
            }
            return snapshot;
        },
        buildWalletTotalsFromCartData: function (totals) {
            totals = totals || {};
            const orderBalanceCents = this.normalizeWalletAmountCents(totals.order_total);
            return {
                order_total: orderBalanceCents / 100,
                order_balance: orderBalanceCents,
                shipping_total: this.normalizeWalletAmountCents(totals.shipping_total),
                discounts_total: this.normalizeWalletAmountCents(totals.discount_total),
                fees_total: this.normalizeWalletAmountCents(totals.fee_total),
                taxes_total: this.normalizeWalletAmountCents(totals.tax_total),
            };
        },
        normalizeWalletShippingOptions: function (shippingOptions) {
            return (shippingOptions || []).map(function (option) {
                return {
                    id: option.id || option.rate_id || '',
                    label: option.label || '',
                    amount: this.normalizeWalletAmountCents(option.amount),
                    is_selected: !!option.is_selected,
                };
            }.bind(this));
        },
        buildOrderDataFromCartData: function (requirements, cartData) {
            const orderData = {};
            for (const requirement of requirements) {
                switch (requirement) {
                    case 'customer': {
                        const billingAddress = this.resolveBillingAddressForCustomerRequirement(cartData);
                        const customer = cartData.customer || {};
                        const email = customer.email || customer.email_address || '';
                        orderData[requirement] = {
                            first_name: billingAddress.first_name || billingAddress.firstname || '',
                            last_name: billingAddress.last_name || billingAddress.lastname || '',
                            email_address: email,
                        };
                        break;
                    }
                    case 'items':
                        orderData[requirement] = (cartData.cart_items || []).map(function (item) {
                            return {
                                amount: this.normalizeWalletAmountCents(item.price),
                                label: item.title || item.name || '',
                            };
                        }.bind(this));
                        break;
                    case 'billing_address':
                        orderData[requirement] = cartData.billing_address;
                        break;
                    case 'shipping_address':
                        orderData[requirement] = cartData.shipping_address;
                        break;
                    case 'shipping_options':
                        orderData[requirement] = this.normalizeWalletShippingOptions(cartData.shipping_options);
                        break;
                    case 'totals':
                        orderData[requirement] = this.buildWalletTotalsFromCartData(cartData.totals || {});
                        break;
                }
            }
            return orderData;
        },
        isWalletCheckoutPayment: function (paymentPayload) {
            const walletType = paymentPayload
                && paymentPayload.payment_data
                && paymentPayload.payment_data.payment_type;
            return walletType === 'apple' || walletType === 'google';
        },
        isWalletExpressPayFlow: function (paymentType, paymentPayload) {
            return paymentType === 'apple'
                || paymentType === 'google'
                || this.isWalletCheckoutPayment(paymentPayload);
        },
        cloneWalletPayload: function (paymentPayload) {
            try {
                return JSON.parse(JSON.stringify(paymentPayload));
            } catch (e) {
                return paymentPayload;
            }
        },
        cacheWalletOrderId: function (orderId) {
            if (orderId) {
                window.bold.lastWalletEpsOrderId = String(orderId);
            }
        },
        resolveWalletOrderId: function (paymentPayload, paymentInformation) {
            const data = paymentPayload && paymentPayload.payment_data ? paymentPayload.payment_data : {};
            if (data.order_id) {
                return String(data.order_id);
            }
            if (data.id) {
                return String(data.id);
            }
            if (paymentInformation) {
                if (paymentInformation.order_id) {
                    return String(paymentInformation.order_id);
                }
                if (paymentInformation.payment_id) {
                    return String(paymentInformation.payment_id);
                }
            }
            if (window.bold.lastWalletEpsOrderId) {
                return window.bold.lastWalletEpsOrderId;
            }
            return null;
        },
        normalizeWalletAddressFields: function (addressData) {
            if (!addressData) {
                return null;
            }
            const street = [];
            if (addressData.addressLines && addressData.addressLines.length) {
                street.push(addressData.addressLines[0]);
                if (addressData.addressLines[1]) {
                    street.push(addressData.addressLines[1]);
                }
            } else {
                street.push(
                    addressData.address_line_1 || addressData.address_line1 || addressData.address1 || ''
                );
                const line2 = addressData.address_line_2 || addressData.address_line2 || addressData.address2;
                if (line2) {
                    street.push(line2);
                }
            }
            return {
                email: addressData.email || addressData.emailAddress || '',
                firstname: addressData.first_name || addressData.givenName || 'Unknown',
                lastname: addressData.last_name || addressData.familyName || 'Person',
                street: street,
                city: addressData.city || addressData.locality || '',
                postcode: addressData.postal_code || addressData.postalCode || '',
                country_id: addressData.country_code || addressData.countryCode || 'US',
                telephone: addressData.phone || addressData.phoneNumber || '5555551234',
                region: addressData.state || addressData.administrativeArea || '',
            };
        },
        postWalletForm: function (url, parameters) {
            return new Promise(function (resolve, reject) {
                new Ajax.Request(url, {
                    method: 'post',
                    parameters: parameters,
                    onSuccess: function (transport) {
                        try {
                            resolve(transport.responseText.evalJSON());
                        } catch (e) {
                            resolve({});
                        }
                    },
                    onFailure: function () {
                        reject(new Error('Request failed: ' + url));
                    }
                });
            });
        },
        getCheckoutPlaceOrderForm: function () {
            const selectors = [
                '#firecheckout-form',
                'form#checkout-form',
                'form[name="firecheckout"]',
                'form[action*="firecheckout"]',
            ];
            let i;
            for (i = 0; i < selectors.length; i++) {
                const match = document.querySelector(selectors[i]);
                if (match && match.tagName === 'FORM') {
                    return match;
                }
            }
            const paymentSection = document.getElementById('co-payment-form');
            if (paymentSection) {
                if (paymentSection.tagName === 'FORM') {
                    return paymentSection;
                }
                if (paymentSection.closest) {
                    const parentForm = paymentSection.closest('form');
                    if (parentForm) {
                        return parentForm;
                    }
                }
            }
            const boldPaymentForm = document.getElementById('payment_form_bold');
            if (boldPaymentForm && boldPaymentForm.closest) {
                return boldPaymentForm.closest('form');
            }
            return null;
        },
        handleWalletSaveOrderResult: function (result) {
            const cfg = window.bold.walletCheckoutConfig;
            if (!result) {
                return;
            }
            if (result.error_messages) {
                const message = Array.isArray(result.error_messages)
                    ? result.error_messages.join('\n')
                    : result.error_messages;
                throw new Error(message);
            }
            if (result.error && result.message) {
                throw new Error(result.message);
            }
            if (result.redirect) {
                window.location.href = result.redirect;
                return;
            }
            if (result.success) {
                window.location.href = cfg.successUrl;
                return;
            }
            if (result.goto_section && typeof checkout !== 'undefined' && checkout.gotoSection) {
                checkout.gotoSection(result.goto_section, result.redirect);
                throw new Error(
                    result.message || 'Please complete all required checkout fields before placing your order.'
                );
            }
        },
        postWalletSaveOrder: async function (orderId) {
            const cfg = window.bold.walletCheckoutConfig;
            const form = this.getCheckoutPlaceOrderForm();
            let parameters = 'form_key=' + encodeURIComponent(cfg.formKey);
            if (form && typeof Form !== 'undefined' && Form.serialize) {
                parameters = Form.serialize(form);
                if (!/[&?]form_key=/.test(parameters)) {
                    parameters += '&form_key=' + encodeURIComponent(cfg.formKey);
                }
            }
            parameters += '&payment[method]=bold';
            parameters += '&payment[additional_data][order_id]=' + encodeURIComponent(orderId);
            const result = await new Promise(function (resolve, reject) {
                new Ajax.Request(cfg.saveOrderUrl, {
                    method: 'post',
                    postBody: parameters,
                    contentType: 'application/x-www-form-urlencoded',
                    onSuccess: function (transport) {
                        try {
                            resolve(transport.responseText.evalJSON());
                        } catch (e) {
                            resolve({});
                        }
                    },
                    onFailure: function () {
                        reject(new Error('Request failed: ' + cfg.saveOrderUrl));
                    }
                });
            });
            this.handleWalletSaveOrderResult(result);
        },
        applyWalletAddressToQuote: async function (addressType, addressData) {
            const normalized = this.normalizeWalletAddressFields(addressData);
            if (!normalized) {
                return;
            }
            const cfg = window.bold.walletCheckoutConfig;
            const params = {
                form_key: cfg.formKey,
                source: 'bold_wallet_checkout'
            };
            const prefix = addressType + '[';
            params[prefix + 'firstname]'] = normalized.firstname;
            params[prefix + 'lastname]'] = normalized.lastname;
            params[prefix + 'street][]'] = normalized.street[0] || '';
            if (normalized.street[1]) {
                params[prefix + 'street][]'] = normalized.street[1];
            }
            params[prefix + 'city]'] = normalized.city;
            params[prefix + 'postcode]'] = normalized.postcode;
            params[prefix + 'country_id]'] = normalized.country_id;
            params[prefix + 'telephone]'] = normalized.telephone;
            params[prefix + 'region]'] = normalized.region;
            if (normalized.email) {
                params[prefix + 'email]'] = normalized.email;
            }
            const url = addressType === 'shipping' ? cfg.saveShippingUrl : cfg.saveBillingUrl;
            await this.postWalletForm(url, params);
        },
        applyWalletAddressesFromPayload: async function (paymentPayload) {
            const data = paymentPayload.payment_data || {};
            if (!window.bold.walletCheckoutConfig.quoteIsVirtual && data.shipping_address) {
                await this.applyWalletAddressToQuote('shipping', data.shipping_address);
            }
            if (data.billing_address) {
                await this.applyWalletAddressToQuote('billing', data.billing_address);
            }
        },
        applyWalletShippingFromPayload: async function (paymentPayload) {
            const cfg = window.bold.walletCheckoutConfig;
            if (cfg.quoteIsVirtual) {
                return;
            }
            const shippingOptions = paymentPayload.payment_data && paymentPayload.payment_data.shipping_options;
            if (!shippingOptions) {
                return;
            }
            let optionId = null;
            if (Array.isArray(shippingOptions)) {
                const selected = shippingOptions.find(function (option) {
                    return option && option.is_selected;
                }) || shippingOptions[0];
                if (selected) {
                    optionId = selected.id || selected.identifier || selected.rate_id;
                }
            } else {
                optionId = shippingOptions.id || shippingOptions.identifier || shippingOptions.rate_id;
            }
            if (!optionId || optionId === 'shipping_option_unselected') {
                return;
            }
            await this.postWalletForm(cfg.saveShippingMethodUrl, {
                form_key: cfg.formKey,
                shipping_method: optionId
            });
        },
        handleWalletCreatePaymentOrder: async function (paymentPayload) {
            if (this.walletPayCreateInFlight && this.walletPayCreatePromise) {
                return this.walletPayCreatePromise;
            }
            this.walletPayCreateInFlight = true;
            this.walletPayCreatePromise = (async () => {
                try {
                    const payload = this.cloneWalletPayload(paymentPayload);
                    await this.applyWalletAddressesFromPayload(payload);
                    const createOrderResult = await this.createExpressOrder(payload);
                    const orderId = createOrderResult.order_id;
                    this.cacheWalletOrderId(orderId);
                    return {payment_data: {id: orderId, order_id: orderId}};
                } catch (e) {
                    window.bold.walletPayOrderLock = false;
                    this.walletCreateOrderConsumed = false;
                    throw e;
                } finally {
                    this.walletPayCreateInFlight = false;
                    this.walletPayCreatePromise = null;
                }
            })();
            return this.walletPayCreatePromise;
        },
        handleWalletUpdatePaymentOrder: async function (paymentPayload) {
            const payload = this.cloneWalletPayload(paymentPayload);
            this.walletCheckoutSessionActive = true;
            window.bold.walletQuoteSyncInProgress = true;
            const paymentData = payload.payment_data || {};
            if (paymentData.order_id || paymentData.id) {
                this.cacheWalletOrderId(paymentData.order_id || paymentData.id);
            }
            try {
                if (!window.bold.walletCheckoutConfig.quoteIsVirtual) {
                    if (paymentData.shipping_address) {
                        await this.applyWalletAddressToQuote('shipping', paymentData.shipping_address);
                    }
                    if (paymentData.shipping_options || paymentData.shipping_address) {
                        await this.applyWalletShippingFromPayload(payload);
                    }
                }
                if (paymentData.billing_address) {
                    await this.applyWalletAddressToQuote('billing', paymentData.billing_address);
                }
                try {
                    await this.refreshCartData();
                } catch (e) {
                    console.warn('[Bold] Could not refresh cart data during wallet update', e);
                }
                const requirements = payload.require_order_data || payload.requirements;
                const orderData = this.onRequireOrderDataCallback(requirements);
                if (!orderData || typeof orderData !== 'object') {
                    return {totals: this.buildWalletTotalsFromCartData(this.ensureCartDataForOrderData(null).totals)};
                }
                if (!orderData.totals) {
                    const cartData = this.ensureCartDataForOrderData(this.getCachedCartData());
                    orderData.totals = this.buildWalletTotalsFromCartData(cartData.totals);
                }
                return orderData;
            } finally {
                window.bold.walletQuoteSyncInProgress = false;
            }
        },
        handleWalletApprovePaymentOrder: async function (paymentPayload, paymentInformation) {
            try {
                const orderId = this.resolveWalletOrderId(paymentPayload, paymentInformation);
                if (!orderId) {
                    throw new Error('Wallet order id is missing');
                }
                this.cacheWalletOrderId(orderId);
                await this.applyWalletAddressesFromPayload(paymentPayload);
                await this.applyWalletShippingFromPayload(paymentPayload);
                const method = window.bold.boldPaymentMethod;
                if (!method) {
                    throw new Error('Bold payment method is not initialized');
                }
                method.paymentId = orderId;
                await method.placeWalletOrder();
            } finally {
                this.clearWalletCheckoutSession();
            }
        },
        createExpressOrder: function (paymentPayload) {
            if (this.createExpressOrderInFlight && this.createExpressOrderPromise) {
                return this.createExpressOrderPromise;
            }
            this.createExpressOrderInFlight = true;
            const cfg = window.bold.walletCheckoutConfig;
            const gatewayId = paymentPayload && paymentPayload.gateway_id;
            this.createExpressOrderPromise = new Promise((resolve, reject) => {
                new Ajax.Request(cfg.createOrderUrl, {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json',
                    },
                    parameters: {
                        form_key: cfg.formKey,
                        quote_id: cfg.quoteId,
                        gateway_id: gatewayId,
                        shipping_strategy: (paymentPayload && paymentPayload.shipping_strategy) || 'dynamic'
                    },
                    onSuccess: (response) => {
                        try {
                            resolve(JSON.parse(response.responseText));
                        } catch (e) {
                            reject(e);
                        }
                    },
                    onFailure: () => {
                        reject(new Error('Failed to create express order'));
                    },
                    onComplete: () => {
                        this.createExpressOrderInFlight = false;
                        this.createExpressOrderPromise = null;
                    }
                });
            });
            return this.createExpressOrderPromise;
        },
        onRequireOrderDataCallback: function (requirements) {
            const normalizedRequirements = this.normalizeOrderDataRequirements(requirements);
            const cartData = this.ensureCartDataForOrderData(this.getCachedCartData());
            try {
                const orderData = this.buildOrderDataFromCartData(normalizedRequirements, cartData) || {};
                if (!orderData.totals) {
                    orderData.totals = this.buildWalletTotalsFromCartData(cartData.totals);
                }
                return orderData;
            } catch (e) {
                console.error('[Bold] onRequireOrderData failed', e);
                const fallbackCart = this.ensureCartDataForOrderData(this.buildMinimalCartDataFallback());
                const orderData = this.buildOrderDataFromCartData(normalizedRequirements, fallbackCart) || {};
                if (!orderData.totals) {
                    orderData.totals = this.buildWalletTotalsFromCartData(fallbackCart.totals);
                }
                return orderData;
            }
        },
        buildMinimalCartDataFallback: function () {
            const emailEl = document.getElementById('billing:email');
            const firstEl = document.getElementById('billing:firstname');
            const lastEl = document.getElementById('billing:lastname');
            const orderTotalCents = this.readCheckoutGrandTotalCents();
            return {
                customer: {
                    email_address: emailEl ? emailEl.value : '',
                    first_name: firstEl ? firstEl.value : '',
                    last_name: lastEl ? lastEl.value : '',
                },
                billing_address: {},
                cart_items: [],
                totals: {
                    order_total: orderTotalCents,
                    shipping_total: 0,
                    discount_total: 0,
                    tax_total: 0,
                    fee_total: 0,
                },
                shipping_options: [],
            };
        },

    });

    /**
     * Bold payment method — single instance in base.phtml (Firecheckout AJAX reloads bold_method markup only).
     */
    const BoldPaymentMethod = Class.create({
        paymentMethodElement: null,
        paymentCheckbox: null,
        isAvailable: <?php echo $this->isAvailable(); ?>,
        isFastlaneAvailable: <?php echo $this->isFastlaneAvailable(); ?>,
        paymentId: null,
        paymentRuntimeVersion: 0,
        isTokenizing: false,
        isCompletingPayment: false,
        tokenizeCooldownUntil: 0,
        lastTerminalFailureAt: 0,
        spiEventsSubscribed: false,
        checkoutSaveWrapped: false,
        paymentSaveWrapped: false,
        renderPaymentFormTimer: null,
        uiRenderRetryScheduled: false,
        initialize: async function () {
            this.syncPaymentRuntimeVersion();
            if (!this.isAvailable) {
                this.logBoldAvailabilityState('initialize');
            }
            if (!this.isBoldPaymentAvailableInCheckout()) {
                return;
            }
            await this.waitForPaymentFormDom();
            this.dedupePaymentFormBoldElements();
            this.refreshDomReferences();
            if (!this.getPaymentFormBold()) {
                this.schedulePaymentFormBootstrapRetry();
                return;
            }
            await this.waitForBaseInstance();
            if (!this.hasValidBaseInstance()) {
                this.schedulePaymentFormBootstrapRetry();
                return;
            }
            this.syncBoldAsCurrentPaymentMethod();
            this.subscribeToSpiEvents();
            this.waitForPaymentInitialization();
            this.waitForCheckoutInitialization();
            if (this.shouldShowBoldPaymentForm()) {
                this.ensurePaymentFormVisible();
                this.scheduleRenderPaymentForm(false, false);
            }
        },
        schedulePaymentFormBootstrapRetry: function () {
            if (this.bootstrapRetryTimer) {
                return;
            }
            let attempts = 0;
            const retry = async () => {
                this.bootstrapRetryTimer = null;
                if (!this.isCurrentRuntime()) {
                    return;
                }
                attempts += 1;
                await this.waitForPaymentFormDom();
                this.dedupePaymentFormBoldElements();
                this.refreshDomReferences();
                if (!this.getPaymentFormBold()) {
                    if (attempts < 50) {
                        this.bootstrapRetryTimer = setTimeout(retry, 200);
                    }
                    return;
                }
                await this.waitForBaseInstance();
                if (!this.hasValidBaseInstance()) {
                    if (attempts < 50) {
                        this.bootstrapRetryTimer = setTimeout(retry, 200);
                    }
                    return;
                }
                this.syncBoldAsCurrentPaymentMethod();
                if (this.isBoldPaymentActive()) {
                    this.ensurePaymentFormVisible();
                    this.scheduleRenderPaymentForm(true, false);
                }
            };
            this.bootstrapRetryTimer = setTimeout(retry, 200);
        },
        isBoldPaymentAvailableInCheckout: function () {
            return !!(document.getElementById('p_method_bold') || this.getPaymentFormBold());
        },
        shouldShowBoldPaymentForm: function () {
            if (!this.isBoldPaymentAvailableInCheckout()) {
                return false;
            }
            const boldInput = document.getElementById('p_method_bold');
            const methods = document.querySelectorAll('input[name="payment[method]"]');
            if (!methods.length) {
                return !!this.getPaymentFormBold();
            }
            if (!boldInput) {
                return false;
            }
            const otherMethodSelected = Array.from(methods).some(function (input) {
                return input.checked && input.value !== 'bold';
            });
            if (otherMethodSelected) {
                return false;
            }
            if (boldInput.checked) {
                return true;
            }
            if (methods.length === 1 && boldInput.value === 'bold') {
                return true;
            }
            if (typeof payment !== 'undefined' && payment.currentMethod === 'bold') {
                return true;
            }
            if (typeof payment !== 'undefined' && payment.lastUsedMethod === 'bold' && !otherMethodSelected) {
                return true;
            }
            return false;
        },
        isBoldPaymentActive: function () {
            return this.shouldShowBoldPaymentForm();
        },
        isSpiFrameReady: function () {
            const form = this.getPaymentFormBold();
            if (!form) {
                return false;
            }
            if (!this.shouldShowBoldPaymentForm() && form.style.display === 'none') {
                return false;
            }
            const frame = this.getActiveSpiFrame();
            return !!(frame && form.contains(frame) && frame.offsetHeight > 0);
        },
        /** SPI/card iframe visible — wallets may still be loading in parallel. */
        isPaymentUiReady: function () {
            return this.isSpiFrameReady();
        },
        isWalletButtonsUiReady: function () {
            if (!this.shouldRenderCheckoutWalletButtons()) {
                return true;
            }
            return this.hasRenderedPpcpButtons();
        },
        scheduleWalletsIfNeeded: function () {
            if (this.shouldRenderCheckoutWalletButtons() && !this.hasVisibleWalletButtons()) {
                this.scheduleWalletButtonsRender();
            }
        },
        hasVisibleWalletButtons: function () {
            const form = this.getPaymentFormBold();
            const host = this.getPpcpButtonsContainer();
            if (!form || !host || !form.contains(host)) {
                return false;
            }
            const candidates = host.querySelectorAll(
                'iframe, [data-funding-source], .paypal-buttons, apple-pay-button, button, [role="button"]'
            );
            for (let i = 0; i < candidates.length; i++) {
                const el = candidates[i];
                if (el.offsetWidth > 0 && el.offsetHeight > 0) {
                    return true;
                }
            }
            return false;
        },
        hasRenderedPpcpButtons: function () {
            return this.hasVisibleWalletButtons();
        },
        invalidateWalletButtonsForTotalsChange: function () {
            if (!this.shouldRenderCheckoutWalletButtons()) {
                return;
            }
            if (window.bold.baseInstance && window.bold.baseInstance.isWalletCheckoutSessionBusy()) {
                return;
            }
            this.clearPpcpButtonsContent();
        },
        shouldRenderCheckoutWalletButtons: function () {
            return this.shouldShowBoldPaymentForm();
        },
        shouldRenderSpi: function (hardReset) {
            if (hardReset) {
                return true;
            }
            const form = this.getPaymentFormBold();
            if (!form) {
                return false;
            }
            const frame = this.getActiveSpiFrame();
            if (!frame || !form.contains(frame)) {
                return true;
            }
            if (this.shouldShowBoldPaymentForm() && frame.offsetHeight <= 0) {
                return true;
            }
            return false;
        },
        shouldRenderWalletButtons: function (hardReset) {
            if (!this.shouldRenderCheckoutWalletButtons()) {
                return false;
            }
            if (hardReset) {
                return true;
            }
            return !this.hasRenderedPpcpButtons();
        },
        walletButtonsRenderScheduled: false,
        scheduleWalletButtonsRender: function () {
            if (!this.shouldRenderCheckoutWalletButtons() || this.walletButtonsRenderScheduled) {
                return;
            }
            this.walletButtonsRenderScheduled = true;
            const self = this;
            (async function () {
                try {
                    if (!self.isSpiFrameReady()) {
                        self.scheduleRenderPaymentForm(false, false);
                        await self.awaitPendingPaymentRender();
                        const deadline = Date.now() + 15000;
                        while (!self.isSpiFrameReady() && Date.now() < deadline) {
                            await self.delay(100);
                        }
                    }
                    if (self.isSpiFrameReady()) {
                        await self.ensurePpcpWalletButtons();
                    }
                } finally {
                    self.walletButtonsRenderScheduled = false;
                }
            })();
        },
        ensurePpcpWalletButtons: async function () {
            if (!this.isCurrentRuntime() || !this.hasValidBaseInstance() || !this.shouldRenderCheckoutWalletButtons()) {
                return;
            }
            if (this.hasRenderedPpcpButtons()) {
                return;
            }
            this.ensurePpcpButtonsContainer();
            const paymentInstance = await window.bold.baseInstance.getBoldPaymentsInstance();
            await this.renderPpcpWalletButtonsWithRetry(paymentInstance, false);
        },
        registerCheckoutWalletIntentListener: function () {
            if (window.bold.checkoutWalletIntentRegistered) {
                return;
            }
            window.bold.checkoutWalletIntentRegistered = true;
            document.observe('pointerdown', function (evt) {
                if (!evt.isTrusted) {
                    return;
                }
                const method = window.bold.boldPaymentMethod;
                if (!method || !method.shouldRenderCheckoutWalletButtons()) {
                    return;
                }
                const el = evt.element();
                if (!el || !el.closest) {
                    return;
                }
                if (el.closest('#smart-ppcp-buttons') || el.closest('.bold-checkout-wallet-buttons')) {
                    if (window.bold.baseInstance) {
                        window.bold.baseInstance.ppcpCheckoutClicked = true;
                        window.bold.baseInstance.armWalletCheckoutCreate();
                    }
                    method.scheduleWalletButtonsRender();
                }
            });
        },
        syncBoldAsCurrentPaymentMethod: function () {
            const methods = Array.from(document.querySelectorAll('input[name="payment[method]"]'));
            const boldMethod = methods.find(function (input) {
                return input.value === 'bold';
            });
            if (!boldMethod) {
                return;
            }
            const otherMethodSelected = methods.some(function (input) {
                return input.checked && input.value !== 'bold';
            });
            if (otherMethodSelected) {
                return;
            }
            if (!boldMethod.checked) {
                boldMethod.checked = true;
            }
            if (typeof payment !== 'undefined' && payment.switchMethod && payment.currentMethod !== 'bold') {
                payment.switchMethod('bold');
            }
        },
        ensureBoldCheckoutUi: async function (options) {
            options = options || {};
            await this.waitForPaymentFormDom();
            this.dedupePaymentFormBoldElements();
            this.refreshDomReferences();
            if (!this.isBoldPaymentAvailableInCheckout()) {
                return;
            }
            if (!this.shouldShowBoldPaymentForm()) {
                this.hidePaymentFormWhenNotSelected();
                return;
            }
            this.syncBoldAsCurrentPaymentMethod();
            this.ensurePaymentFormVisible();
            if (!this.hasValidBaseInstance()) {
                await this.waitForBaseInstance();
            }
            if (!this.hasValidBaseInstance()) {
                return;
            }
            if (options.refreshWallets) {
                this.invalidateWalletButtonsForTotalsChange();
            }
            if (!options.hardReload && this.isPaymentUiReady() && this.isWalletButtonsUiReady()) {
                this.ensurePpcpButtonsContainer();
                return;
            }
            if (!options.hardReload && this.isPaymentUiReady()) {
                this.ensurePpcpButtonsContainer();
                this.scheduleWalletsIfNeeded();
                if (!options.forceRender && !options.refreshWallets) {
                    return;
                }
            }
            const needsRender = !!options.hardReload
                || !!options.refreshWallets
                || (!!options.forceRender && (!this.isPaymentUiReady() || !this.isWalletButtonsUiReady()));
            if (!needsRender) {
                return;
            }
            await this.awaitPendingPaymentRender();
            if (options.hardReload) {
                this.bumpPaymentRuntimeVersion();
                this.resetPaymentFlowState();
                if (typeof checkout !== 'undefined') {
                    checkout.setLoadWaiting(false);
                }
            }
            await this.renderPaymentForm(
                !!options.hardReload || !!options.refreshWallets,
                !!options.hardReload
            );
            if (this.shouldShowBoldPaymentForm()) {
                this.ensurePaymentFormVisible();
            }
        },
        syncPaymentRuntimeVersion: function () {
            if (typeof window.bold.paymentRuntimeVersion !== 'number') {
                window.bold.paymentRuntimeVersion = 1;
            }
            this.paymentRuntimeVersion = window.bold.paymentRuntimeVersion;
        },
        bumpPaymentRuntimeVersion: function () {
            if (typeof window.bold.paymentRuntimeVersion !== 'number') {
                window.bold.paymentRuntimeVersion = 1;
            }
            window.bold.paymentRuntimeVersion += 1;
            this.paymentRuntimeVersion = window.bold.paymentRuntimeVersion;
            return this.paymentRuntimeVersion;
        },
        isCurrentRuntime: function () {
            return window.bold.boldPaymentMethod === this
                && this.paymentRuntimeVersion === window.bold.paymentRuntimeVersion;
        },
        resetPaymentFlowState: function () {
            this.paymentId = null;
            this.isTokenizing = false;
            this.isCompletingPayment = false;
        },
        canAttemptTokenize: function () {
            if (this.isTokenizing || this.isCompletingPayment) {
                return false;
            }
            if (this.tokenizeCooldownUntil && Date.now() < this.tokenizeCooldownUntil) {
                return false;
            }
            return true;
        },
        delay: function (ms) {
            return new Promise((resolve) => {
                setTimeout(resolve, ms);
            });
        },
        getPaymentMethodLoadRoot: function () {
            return document.getElementById('checkout-payment-method-load')
                || document.getElementById('payment-method');
        },
        getAllPaymentFormBoldElements: function () {
            return Array.from(document.querySelectorAll('[id="payment_form_bold"]'));
        },
        dedupePaymentFormBoldElements: function () {
            const forms = this.getAllPaymentFormBoldElements();
            if (forms.length <= 1) {
                return;
            }
            const root = this.getPaymentMethodLoadRoot();
            let keepForm = forms[forms.length - 1];
            if (root) {
                const inRoot = forms.filter((form) => root.contains(form));
                if (inRoot.length) {
                    keepForm = inRoot[inRoot.length - 1];
                }
            }
            forms.forEach((form) => {
                if (form !== keepForm) {
                    form.remove();
                }
            });
            document.querySelectorAll('[id="spi_frame_payment_form_bold"]').forEach((frame) => {
                if (!keepForm.contains(frame)) {
                    frame.remove();
                }
            });
        },
        getPaymentFormBold: function () {
            this.dedupePaymentFormBoldElements();
            const root = this.getPaymentMethodLoadRoot();
            if (root) {
                const inRoot = root.querySelector('[id="payment_form_bold"]');
                if (inRoot) {
                    return inRoot;
                }
            }
            const forms = this.getAllPaymentFormBoldElements();
            return forms.length ? forms[forms.length - 1] : null;
        },
        refreshDomReferences: function () {
            this.paymentCheckbox = document.getElementById('p_method_bold');
            this.paymentMethodElement = this.paymentCheckbox
                ? this.paymentCheckbox.parentNode
                : null;
        },
        waitForPaymentFormDom: function () {
            return new Promise((resolve) => {
                if (this.getPaymentFormBold()) {
                    resolve();
                    return;
                }
                const intervalId = setInterval(() => {
                    if (this.getPaymentFormBold()) {
                        clearInterval(intervalId);
                        resolve();
                    }
                }, 100);
                setTimeout(() => {
                    clearInterval(intervalId);
                    resolve();
                }, 10000);
            });
        },
        hasValidBaseInstance: function () {
            return window.bold
                && window.bold.baseInstance
                && typeof window.bold.baseInstance.getBoldPaymentsInstance === 'function';
        },
        waitForBaseInstance: function () {
            return new Promise((resolve) => {
                if (this.hasValidBaseInstance()) {
                    resolve();
                    return;
                }
                const intervalId = setInterval(() => {
                    if (this.hasValidBaseInstance()) {
                        clearInterval(intervalId);
                        resolve();
                    }
                }, 100);
                setTimeout(() => {
                    clearInterval(intervalId);
                    resolve();
                }, 10000);
            });
        },
        getActiveSpiFrame: function () {
            const form = this.getPaymentFormBold();
            return form ? form.querySelector('[id="spi_frame_payment_form_bold"]') : null;
        },
        getPpcpButtonsContainer: function () {
            const form = this.getPaymentFormBold();
            return form ? form.querySelector('[id="smart-ppcp-buttons"]') : document.getElementById('smart-ppcp-buttons');
        },
        ensurePpcpButtonsContainer: function () {
            const form = this.getPaymentFormBold();
            if (!form) {
                return null;
            }
            let host = this.getPpcpButtonsContainer();
            if (!host) {
                host = document.createElement('div');
                host.id = 'smart-ppcp-buttons';
                host.className = 'bold-ppcp-wallet-host';
                const inner = document.createElement('div');
                inner.id = 'paypal-button-container';
                host.appendChild(inner);
            } else if (!host.querySelector('[id="paypal-button-container"]')) {
                const inner = document.createElement('div');
                inner.id = 'paypal-button-container';
                host.appendChild(inner);
            }
            if (host.parentNode !== form) {
                form.appendChild(host);
            }
            const spiFrame = this.getActiveSpiFrame();
            if (spiFrame && host.nextSibling !== spiFrame) {
                form.insertBefore(host, spiFrame);
            } else if (!spiFrame && form.firstChild !== host) {
                form.insertBefore(host, form.firstChild);
            }
            host.style.display = '';
            return host;
        },
        clearPpcpButtonsContent: function () {
            const host = this.getPpcpButtonsContainer();
            if (!host) {
                return;
            }
            const inner = host.querySelector('[id="paypal-button-container"]') || host;
            while (inner.firstChild) {
                inner.removeChild(inner.firstChild);
            }
        },
        needsPpcpButtonsRender: function () {
            return !!this.getPaymentFormBold() && !this.hasRenderedPpcpButtons();
        },
        dedupeSpiFrames: function () {
            const form = this.getPaymentFormBold();
            if (!form) {
                return;
            }
            const frames = Array.from(form.querySelectorAll('[id="spi_frame_payment_form_bold"]'));
            frames.slice(0, -1).forEach((frame) => frame.remove());
        },
        clearSpiFromDom: function () {
            this.resetPaymentFormContainer(false);
        },
        resetPaymentFormContainer: function (resetSdk) {
            if (typeof resetSdk === 'undefined') {
                resetSdk = true;
            }
            if (window.bold.baseInstance && window.bold.baseInstance.shouldBlockWalletDomTeardown()) {
                window.bold.baseInstance.queuePaymentSectionSyncAfterWallet({hardReload: true, forceRender: true});
                return;
            }
            if (window.bold.baseInstance) {
                window.bold.baseInstance.clearWalletCheckoutSession();
            }
            const form = this.getPaymentFormBold();
            if (!form) {
                if (resetSdk && window.bold.baseInstance) {
                    window.bold.baseInstance.resetPaymentsInstance();
                }
                return;
            }
            while (form.firstChild) {
                form.removeChild(form.firstChild);
            }
            document.querySelectorAll('[id="spi_frame_payment_form_bold"]').forEach((frame) => {
                frame.remove();
            });
            const host = document.createElement('div');
            host.id = 'smart-ppcp-buttons';
            host.className = 'bold-ppcp-wallet-host';
            const inner = document.createElement('div');
            inner.id = 'paypal-button-container';
            host.appendChild(inner);
            form.appendChild(host);
            if (resetSdk && window.bold.baseInstance) {
                window.bold.baseInstance.resetPaymentsInstance();
            }
        },
        isSdkAlreadyRenderedMessage: function (error) {
            const message = error && error.message ? error.message : String(error);
            return message.toLowerCase().indexOf('already rendered') !== -1;
        },
        isSpiAlreadyRenderedError: function (error) {
            return this.isSdkAlreadyRenderedMessage(error);
        },
        isPaymentRenderReady: function () {
            return !!this.getActiveSpiFrame();
        },
        canSkipPaymentRender: function (forceReset) {
            if (forceReset || !this.isCurrentRuntime()) {
                return false;
            }
            if (!this.isSpiFrameReady()) {
                return false;
            }
            if (this.shouldRenderCheckoutWalletButtons() && !this.hasVisibleWalletButtons()) {
                return false;
            }
            return true;
        },
        awaitPendingPaymentRender: function () {
            if (window.bold.paymentRenderPromise) {
                return window.bold.paymentRenderPromise.catch(function () {});
            }
            return Promise.resolve();
        },
        renderPpcpWalletButtons: async function (paymentInstance, force) {
            if (!paymentInstance || typeof paymentInstance.renderWalletPayments !== 'function') {
                return false;
            }
            if (!force && window.bold.baseInstance && window.bold.baseInstance.isWalletCheckoutSessionBusy()) {
                return this.hasRenderedPpcpButtons();
            }
            if (!force && this.hasRenderedPpcpButtons()) {
                return true;
            }
            if (!force && window.bold.walletButtonsRendering) {
                return this.hasRenderedPpcpButtons();
            }
            if (force) {
                this.clearPpcpButtonsContent();
            } else if (!this.needsPpcpButtonsRender()) {
                return this.hasRenderedPpcpButtons();
            }
            window.bold.walletButtonsRendering = true;
            window.bold.walletSdkMounting = true;
            this.ensurePpcpButtonsContainer();
            const shopName = window.bold.baseInstance && window.bold.baseInstance.groupLabel
                ? window.bold.baseInstance.groupLabel
                : '';
            try {
                await paymentInstance.renderWalletPayments('smart-ppcp-buttons', {
                    allowedCountries: ['US', 'CA'],
                    fastlane: !!this.isFastlaneAvailable,
                    isPhoneRequired: true,
                    pageSource: 'checkout',
                    shopName: shopName
                });
                return this.hasRenderedPpcpButtons();
            } catch (e) {
                if (this.isSdkAlreadyRenderedMessage(e)) {
                    const form = this.getPaymentFormBold();
                    const staleHost = this.getPpcpButtonsContainer();
                    if (staleHost && form) {
                        staleHost.remove();
                    }
                    this.ensurePpcpButtonsContainer();
                    await this.delay(200);
                    try {
                        await paymentInstance.renderWalletPayments('smart-ppcp-buttons', {
                            allowedCountries: ['US', 'CA'],
                            fastlane: !!this.isFastlaneAvailable,
                            isPhoneRequired: true,
                            pageSource: 'checkout',
                            shopName: shopName
                        });
                        return this.hasRenderedPpcpButtons();
                    } catch (retryError) {
                        console.warn('[Bold] renderWalletPayments retry failed', retryError);
                        return false;
                    }
                }
                console.warn('[Bold] renderWalletPayments failed', e);
                return false;
            } finally {
                window.bold.walletButtonsRendering = false;
                window.bold.walletSdkMounting = false;
                window.bold.walletPayMountCooldownUntil = Date.now() + 2000;
            }
        },
        renderSpiPayments: async function (paymentInstance, renderGeneration) {
            const spiOptions = {
                fastlane: false,
                shouldRenderSpiFrame: true
            };
            try {
                await paymentInstance.renderPayments('payment_form_bold', spiOptions);
                return true;
            } catch (e) {
                if (!this.isSdkAlreadyRenderedMessage(e)) {
                    throw e;
                }
                console.warn('[Bold] SPI stale SDK state detected, resetting container and retrying');
                this.resetPaymentFormContainer(true);
                await this.delay(400);
                if (!this.isCurrentRuntime() || renderGeneration !== window.bold.spiRenderGeneration) {
                    return false;
                }
                const retryInstance = await window.bold.baseInstance.getBoldPaymentsInstance();
                await retryInstance.renderPayments('payment_form_bold', spiOptions);
                return true;
            }
        },
        renderPpcpWalletButtonsWithRetry: async function (paymentInstance, force) {
            const maxAttempts = force ? 3 : 1;
            for (let attempt = 0; attempt < maxAttempts; attempt++) {
                if (!this.isCurrentRuntime()) {
                    return;
                }
                const rendered = await this.renderPpcpWalletButtons(paymentInstance, !!force);
                if (rendered) {
                    return;
                }
                if (attempt < maxAttempts - 1) {
                    await this.delay(300);
                }
            }
        },
        scheduleRenderPaymentForm: function (immediate, forceReset, hardReset) {
            if (typeof hardReset === 'undefined') {
                hardReset = !!forceReset;
            }
            if (typeof window.bold.spiRenderGeneration !== 'number') {
                window.bold.spiRenderGeneration = 0;
            }
            window.bold.spiRenderGeneration += 1;
            const generation = window.bold.spiRenderGeneration;
            if (this.renderPaymentFormTimer) {
                clearTimeout(this.renderPaymentFormTimer);
                this.renderPaymentFormTimer = null;
            }
            const run = () => {
                if (generation !== window.bold.spiRenderGeneration) {
                    return;
                }
                this.renderPaymentForm(!!forceReset, !!hardReset);
            };
            if (immediate) {
                run();
            } else {
                this.renderPaymentFormTimer = setTimeout(run, 200);
            }
        },
        renderPaymentForm: async function (forceReset, hardReset) {
            if (typeof hardReset === 'undefined') {
                hardReset = !!forceReset;
            }
            if (!this.isCurrentRuntime() || !this.hasValidBaseInstance()) {
                return;
            }
            const form = this.getPaymentFormBold();
            if (!form) {
                return;
            }
            if (this.shouldShowBoldPaymentForm()) {
                this.ensurePaymentFormVisible();
            }
            if (this.canSkipPaymentRender(forceReset)) {
                if (this.shouldRenderCheckoutWalletButtons() && !this.hasRenderedPpcpButtons()) {
                    this.scheduleWalletButtonsRender();
                }
                return;
            }
            await this.awaitPendingPaymentRender();
            if (!this.isCurrentRuntime() || this.canSkipPaymentRender(forceReset)) {
                return;
            }
            const renderGeneration = window.bold.spiRenderGeneration;
            const renderTask = (async () => {
                window.bold.spiRenderInProgress = true;
                window.bold.checkoutSdkMounting = true;
                try {
                    if (hardReset) {
                        this.bumpPaymentRuntimeVersion();
                        this.resetPaymentFormContainer(true);
                        await this.delay(300);
                    }
                    if (!this.isCurrentRuntime() || renderGeneration !== window.bold.spiRenderGeneration) {
                        return;
                    }
                    const paymentInstance = await window.bold.baseInstance.getBoldPaymentsInstance();
                    if (!this.isCurrentRuntime() || renderGeneration !== window.bold.spiRenderGeneration) {
                        return;
                    }
                    this.ensurePpcpButtonsContainer();
                    const needsSpi = this.shouldRenderSpi(hardReset);
                    const needsWallets = this.shouldRenderWalletButtons(hardReset);
                    const self = this;
                    const renderSpi = async function () {
                        if (!needsSpi) {
                            return;
                        }
                        if (!self.isFastlaneAvailable) {
                            await self.renderSpiPayments(paymentInstance, renderGeneration);
                            return;
                        }
                        try {
                            await paymentInstance.renderPayments('payment_form_bold', {
                                fastlane: true,
                                shouldRenderSpiFrame: false
                            });
                        } catch (e) {
                            if (self.isSdkAlreadyRenderedMessage(e)) {
                                self.resetPaymentFormContainer(true);
                                await self.delay(400);
                                const retryInstance = await window.bold.baseInstance.getBoldPaymentsInstance();
                                await retryInstance.renderPayments('payment_form_bold', {
                                    fastlane: true,
                                    shouldRenderSpiFrame: false
                                });
                            } else {
                                throw e;
                            }
                        }
                    };
                    const renderWallets = async function () {
                        if (!needsWallets) {
                            return;
                        }
                        await self.renderPpcpWalletButtonsWithRetry(paymentInstance, hardReset || forceReset);
                    };
                    await Promise.all([renderSpi(), renderWallets()]);
                    if (!this.isCurrentRuntime() || renderGeneration !== window.bold.spiRenderGeneration) {
                        return;
                    }
                    this.dedupeSpiFrames();
                    this.ensurePaymentFormVisible();
                } finally {
                    window.bold.spiRenderInProgress = false;
                    window.bold.checkoutSdkMounting = false;
                }
            })();
            window.bold.paymentRenderPromise = renderTask;
            try {
                await renderTask;
            } catch (e) {
                console.error('[Bold] renderPaymentForm failed', e);
            } finally {
                if (window.bold.paymentRenderPromise === renderTask) {
                    window.bold.paymentRenderPromise = null;
                }
                if (this.shouldShowBoldPaymentForm() && !this.hasRenderedPpcpButtons()) {
                    this.scheduleWalletButtonsRender();
                }
                if (this.shouldShowBoldPaymentForm()
                    && !this.isWalletButtonsUiReady()
                    && !this.uiRenderRetryScheduled) {
                    this.uiRenderRetryScheduled = true;
                    const self = this;
                    setTimeout(function () {
                        self.uiRenderRetryScheduled = false;
                    }, 3000);
                    this.scheduleRenderPaymentForm(false, true);
                }
            }
        },
        isBoldPaymentSelected: function () {
            return this.isBoldPaymentActive();
        },
        ensurePaymentFormVisible: function () {
            const form = this.getPaymentFormBold();
            if (!form) {
                return;
            }
            form.style.display = '';
            if (typeof payment !== 'undefined' && payment.changeVisible) {
                payment.changeVisible('bold', false);
            }
        },
        hidePaymentFormWhenNotSelected: function () {
            const form = this.getPaymentFormBold();
            if (!form) {
                return;
            }
            if (typeof payment !== 'undefined' && payment.changeVisible) {
                payment.changeVisible('bold', true);
                return;
            }
            form.style.display = 'none';
        },
        onPaymentSectionReloaded: async function (options) {
            options = options || {};
            await this.waitForPaymentFormDom();
            this.dedupePaymentFormBoldElements();
            this.refreshDomReferences();
            if (!this.getPaymentFormBold()) {
                return;
            }
            if (!this.isAvailable) {
                this.logBoldAvailabilityState('onPaymentSectionReloaded');
                return;
            }
            if (options.hardReload) {
                this.resetPaymentFlowState();
                await this.awaitPendingPaymentRender();
                if (typeof checkout !== 'undefined') {
                    checkout.setLoadWaiting(false);
                }
            }
            await this.waitForBaseInstance();
            this.syncBoldAsCurrentPaymentMethod();
            if (!options.skipRender) {
                this.scheduleRenderPaymentForm(true, !!options.hardReload, !!options.hardReload);
            }
            if (this.isBoldPaymentActive()) {
                this.ensurePaymentFormVisible();
            } else {
                this.hidePaymentFormWhenNotSelected();
            }
            this.waitForPaymentInitialization();
            this.waitForCheckoutInitialization();
        },
        refreshPaymentFormAfterTotalsChange: async function () {
            if (!this.isCurrentRuntime() || !this.isBoldPaymentActive()) {
                return;
            }
            await this.waitForPaymentFormDom();
            this.dedupePaymentFormBoldElements();
            this.refreshDomReferences();
            if (!this.getPaymentFormBold()) {
                return;
            }
            this.ensurePaymentFormVisible();
            this.resetPaymentFlowState();
            await this.awaitPendingPaymentRender();
            if (window.bold.baseInstance && typeof window.bold.baseInstance.getCartData === 'function') {
                try {
                    await window.bold.baseInstance.getCartData();
                } catch (e) {
                    console.warn('[Bold] Could not refresh cart data before payment re-render', e);
                }
            }
            await this.renderPaymentForm(false, false);
        },
        onBoldPaymentMethodSelected: function () {
            if (!this.shouldShowBoldPaymentForm()) {
                return;
            }
            this.refreshDomReferences();
            if (!this.getPaymentFormBold()) {
                return;
            }
            this.ensurePaymentFormVisible();
            if (!this.isPaymentUiReady()) {
                this.scheduleRenderPaymentForm(false, false);
            } else {
                this.scheduleWalletsIfNeeded();
            }
        },
        syncPaymentIdToForm: function (orderId) {
            const hostForm = window.bold.baseInstance
                ? window.bold.baseInstance.getCheckoutPlaceOrderForm()
                : null;
            if (!hostForm) {
                return;
            }
            let input = document.getElementById('bold_wallet_payment_order_id');
            if (!input) {
                input = document.createElement('input');
                input.type = 'hidden';
                input.id = 'bold_wallet_payment_order_id';
                input.name = 'payment[additional_data][order_id]';
                hostForm.appendChild(input);
            } else if (input.parentNode !== hostForm) {
                hostForm.appendChild(input);
            }
            input.value = orderId;
        },
        selectBoldPaymentMethod: function () {
            if (typeof payment !== 'undefined' && payment.switchMethod) {
                payment.switchMethod('bold');
                return;
            }
            this.refreshDomReferences();
            if (this.paymentCheckbox) {
                this.paymentCheckbox.checked = true;
            }
        },
        placeWalletOrder: async function () {
            if (!this.paymentId) {
                throw new Error('Wallet payment id is missing');
            }
            this.syncPaymentIdToForm(this.paymentId);
            this.selectBoldPaymentMethod();
            if (window.bold.baseInstance && typeof window.bold.baseInstance.postWalletSaveOrder === 'function') {
                return window.bold.baseInstance.postWalletSaveOrder(this.paymentId);
            }
            if (typeof payment !== 'undefined' && payment.save) {
                payment.save();
                return;
            }
            if (typeof checkout !== 'undefined' && checkout.save && typeof Translator !== 'undefined') {
                checkout.save();
                return;
            }
            throw new Error('Checkout is not ready to place order');
        },
        getBoldPaymentDd: function () {
            return document.getElementById('dd_method_bold')
                || document.querySelector('dd[id="dd_method_bold"]');
        },
        getBoldPaymentDt: function () {
            const radio = document.getElementById('p_method_bold');
            if (!radio) {
                return null;
            }
            return radio.closest ? radio.closest('dt') : null;
        },
        logBoldAvailabilityState: function (context) {
            const build = window.bold.checkoutPaymentBoosterBuild || {};
            console.warn(
                '[Bold CheckoutPaymentBooster] Bold session not ready (' + context + ').',
                'buildId:', build.buildId,
                'p_method_bold:', !!document.getElementById('p_method_bold'),
                'payment_form_bold:', !!document.getElementById('payment_form_bold'),
                'isAvailable flag:', this.isAvailable
            );
        },
        hidePaymentMethod: function () {
            const form = this.getPaymentFormBold();
            if (form) {
                form.style.display = 'none';
                form.classList.add('bold-payment-inactive');
            }
            const dd = this.getBoldPaymentDd();
            if (dd) {
                dd.classList.add('bold-payment-dd-inactive');
            }
            const dt = this.getBoldPaymentDt();
            if (dt) {
                dt.style.display = '';
                dt.style.visibility = 'visible';
            }
        },
        showPaymentMethodInList: function () {
            const dt = this.getBoldPaymentDt();
            if (dt) {
                dt.style.display = '';
                dt.style.visibility = 'visible';
            }
            const dd = this.getBoldPaymentDd();
            if (dd) {
                dd.classList.remove('bold-payment-dd-inactive');
            }
        },
        waitForCheckoutInitialization: function () {
            const intervalId = setInterval(() => {
                if (typeof checkout !== 'undefined' && checkout.save) {
                    this.wrapCheckoutSave();
                    clearInterval(intervalId);
                }
            }, 100);
            setTimeout(() => clearInterval(intervalId), 10000);
        },
        waitForPaymentInitialization: function () {
            const intervalId = setInterval(() => {
                if (typeof payment !== 'undefined' && payment.save) {
                    this.wrapPaymentSave();
                    clearInterval(intervalId);
                }
            }, 100);
            setTimeout(() => clearInterval(intervalId), 10000);
        },
        wrapCheckoutSave: function () {
            if (this.checkoutSaveWrapped) {
                return;
            }
            this.checkoutSaveWrapped = true;
            checkout.save = checkout.save.wrap(function (original) {
                if (!this.isCurrentRuntime() || !this.paymentCheckbox || !this.paymentCheckbox.checked) {
                    return original();
                }
                checkout.validate();
                if (!this.paymentId) {
                    if (!this.canAttemptTokenize()) {
                        return;
                    }
                    checkout.setLoadWaiting('payment', true);
                    this.tokenize();
                    return;
                }
                this.isCompletingPayment = false;
                this.isTokenizing = false;
                checkout.setLoadWaiting(false);
                return original();
            }.bind(this));
        },
        wrapPaymentSave: function () {
            if (this.paymentSaveWrapped) {
                return;
            }
            this.paymentSaveWrapped = true;
            payment.save = payment.save.wrap(function (original) {
                if (!this.isCurrentRuntime() || !this.paymentCheckbox || !this.paymentCheckbox.checked) {
                    return original();
                }
                if (!this.paymentId) {
                    if (typeof checkout !== 'undefined' && checkout.save) {
                        return;
                    }
                    if (!this.canAttemptTokenize()) {
                        return;
                    }
                    checkout.setLoadWaiting('payment', true);
                    this.tokenize();
                    return;
                }
                this.isCompletingPayment = false;
                this.isTokenizing = false;
                checkout.setLoadWaiting(false);
                return original();
            }.bind(this));
        },
        tokenize: async function () {
            if (!this.isCurrentRuntime() || !this.canAttemptTokenize()) {
                return;
            }
            this.dedupeSpiFrames();
            this.isTokenizing = true;
            try {
                const iframe = this.getActiveSpiFrame();
                if (!iframe || !iframe.contentWindow) {
                    console.error('[Bold] SPI iframe is not available');
                    this.isTokenizing = false;
                    checkout.setLoadWaiting(false);
                    return;
                }
                const cartData = await window.bold.baseInstance.getCartData();
                if (!this.isCurrentRuntime()) {
                    this.isTokenizing = false;
                    return;
                }
                iframe.contentWindow.postMessage({
                    actionType: 'ACTION_SPI_TOKENIZE',
                    payload: {
                        customer: cartData.customer,
                        billing_address: cartData.billing_address,
                        shipping_address: cartData.shipping_address || cartData.billing_address,
                        totals: cartData.totals,
                    }
                }, '*');
            } catch (e) {
                this.isTokenizing = false;
                checkout.setLoadWaiting(false);
                console.error('[Bold] tokenize failed', e);
            }
        },
        isMessageFromActiveSpi: function (event) {
            const iframe = this.getActiveSpiFrame();
            return !!(iframe && iframe.contentWindow && event.source === iframe.contentWindow);
        },
        handleSpiTerminalFailure: function (reason) {
            if (!this.isCurrentRuntime()) {
                return;
            }
            if (this.lastTerminalFailureAt && (Date.now() - this.lastTerminalFailureAt) < 1000) {
                return;
            }
            this.lastTerminalFailureAt = Date.now();
            this.tokenizeCooldownUntil = Date.now() + 5000;
            this.resetPaymentFlowState();
            if (typeof checkout !== 'undefined') {
                checkout.setLoadWaiting(false);
            }
            console.warn('[Bold] Payment tokenization failed', reason || '');
        },
        subscribeToSpiEvents: function () {
            if (this.spiEventsSubscribed) {
                return;
            }
            this.spiEventsSubscribed = true;
            window.addEventListener('message', (event) => {
                const data = event.data;
                if (!data || !data.eventType) {
                    return;
                }
                const instance = window.bold.boldPaymentMethod;
                if (!instance || !instance.isCurrentRuntime() || !instance.isMessageFromActiveSpi(event)) {
                    return;
                }
                switch (data.eventType) {
                    case 'EVENT_SPI_TOKENIZED':
                        if (!data.payload || !data.payload.success) {
                            instance.handleSpiTerminalFailure('tokenize_unsuccessful');
                            return;
                        }
                        if (instance.isCompletingPayment) {
                            return;
                        }
                        instance.paymentId = data.payload?.payload?.data?.payment_id;
                        instance.isTokenizing = false;
                        instance.isCompletingPayment = true;
                        if (checkout.save) {
                            checkout.save();
                        } else {
                            payment.save();
                        }
                        break;
                    case 'EVENT_SPI_TOKENIZE_FAILED':
                        instance.handleSpiTerminalFailure('tokenize_failed');
                        break;
                    case 'EVENT_SPI_PAYMENT_ORDER_SCA':
                        instance.isTokenizing = false;
                        checkout.setLoadWaiting(false);
                        break;
                    case 'EVENT_SPI_ENABLE_FULLSCREEN':
                        checkout.setLoadWaiting(false);
                        break;
                    case 'EVENT_SPI_DISABLE_FULLSCREEN':
                        if (instance.tokenizeCooldownUntil && Date.now() < instance.tokenizeCooldownUntil) {
                            checkout.setLoadWaiting(false);
                            break;
                        }
                        if (instance.isTokenizing || instance.isCompletingPayment) {
                            checkout.setLoadWaiting('payment', true);
                        }
                        break;
                    default:
                        if (data.eventType && data.eventType.indexOf('FAIL') !== -1) {
                            instance.handleSpiTerminalFailure(data.eventType);
                        }
                        break;
                }
            });
        },
    });

    window.bold.ensureBoldBaseInstance = function () {
        if (typeof window.bold === 'undefined') {
            window.bold = {};
        }
        const created = !window.bold.baseInstance;
        if (!window.bold.baseInstance) {
            window.bold.baseInstance = new BoldBase();
        }
        if (created && window.bold.baseInstance
            && typeof window.bold.baseInstance.getBoldPaymentsInstance === 'function') {
            document.fire('bold:baseInstanceReady');
        }
        return window.bold.baseInstance;
    };

    window.bold.initBoldPaymentMethod = function () {
        const hasBoldRadio = !!document.getElementById('p_method_bold');
        if (!<?php echo $this->isAvailable(); ?> && !hasBoldRadio) {
            return;
        }
        if (!window.bold.boldPaymentMethod) {
            window.bold.boldPaymentMethod = new BoldPaymentMethod();
        }
        if (hasBoldRadio && window.bold.boldPaymentMethod) {
            window.bold.boldPaymentMethod.showPaymentMethodInList();
        }
    };

    window.bold.ensureBoldPaymentRuntime = function () {
        const hasBoldRadio = !!document.getElementById('p_method_bold');
        if (!<?php echo $this->isAvailable(); ?> && !hasBoldRadio) {
            return null;
        }
        window.bold.ensureBoldBaseInstance();
        if (!window.bold.boldPaymentMethod) {
            window.bold.initBoldPaymentMethod();
        }
        return window.bold.boldPaymentMethod || null;
    };

    window.bold.checkoutSectionsTriggeringBoldSync = [
        'payment-method',
        'shipping-method',
        'cart',
        'total',
        'billing',
        'review',
        'coupon-discount'
    ];

    window.bold.scheduleBoldPaymentSectionSync = function (options) {
        options = options || {};
        const instance = window.bold.boldPaymentMethod;
        const baseInstance = window.bold.baseInstance;
        if (baseInstance && baseInstance.isWalletCheckoutSessionBusy()) {
            baseInstance.queuePaymentSectionSyncAfterWallet(options);
            return;
        }
        if (options.refreshWallets && baseInstance) {
            baseInstance.resetWalletSessionForCheckoutRefresh();
        }
        if (!options.hardReload && !options.refreshWallets && instance
            && instance.isPaymentUiReady() && instance.isWalletButtonsUiReady()) {
            instance.ensurePaymentFormVisible();
            instance.ensurePpcpButtonsContainer();
            return;
        }
        if (window.bold.paymentSyncInProgress) {
            if (!window.bold.pendingPaymentSectionSync) {
                window.bold.pendingPaymentSectionSync = {
                    hardReload: false,
                    forceRender: false,
                    refreshWallets: false
                };
            }
            if (options.hardReload) {
                window.bold.pendingPaymentSectionSync.hardReload = true;
            }
            if (options.forceRender) {
                window.bold.pendingPaymentSectionSync.forceRender = true;
            }
            if (options.refreshWallets) {
                window.bold.pendingPaymentSectionSync.refreshWallets = true;
            }
            return;
        }
        if (!window.bold.pendingPaymentSectionSync) {
            window.bold.pendingPaymentSectionSync = {
                hardReload: false,
                forceRender: false,
                refreshWallets: false
            };
        }
        if (options.hardReload) {
            window.bold.pendingPaymentSectionSync.hardReload = true;
            window.bold.pendingPaymentSectionSync.forceRender = true;
        }
        if (options.forceRender) {
            window.bold.pendingPaymentSectionSync.forceRender = true;
        }
        if (options.refreshWallets) {
            window.bold.pendingPaymentSectionSync.refreshWallets = true;
            window.bold.pendingPaymentSectionSync.forceRender = true;
        }
        if (window.bold.paymentSectionSyncTimer) {
            clearTimeout(window.bold.paymentSectionSyncTimer);
        }
        window.bold.paymentSectionSyncTimer = setTimeout(function () {
            window.bold.paymentSectionSyncTimer = null;
            window.bold.runBoldPaymentSectionSync();
        }, 150);
    };

    window.bold.runBoldPaymentSectionSync = async function () {
        const pending = window.bold.pendingPaymentSectionSync || {
            hardReload: false,
            forceRender: false,
            refreshWallets: false
        };
        window.bold.pendingPaymentSectionSync = null;
        if (window.bold.paymentSyncInProgress) {
            return;
        }
        const instance = window.bold.ensureBoldPaymentRuntime();
        if (!instance) {
            return;
        }
        window.bold.paymentSyncInProgress = true;
        try {
            if (window.bold.baseInstance && typeof window.bold.baseInstance.refreshCartData === 'function') {
                try {
                    await window.bold.baseInstance.refreshCartData();
                } catch (e) {
                    console.warn('[Bold] Could not refresh cart data before checkout sync', e);
                }
            }
            await instance.ensureBoldCheckoutUi({
                hardReload: !!pending.hardReload,
                forceRender: !!pending.forceRender,
                refreshWallets: !!pending.refreshWallets
            });
            instance.waitForPaymentInitialization();
            instance.waitForCheckoutInitialization();
            window.bold.lastPaymentSyncFinishedAt = Date.now();
            window.bold.lastSyncUiReady = instance.isPaymentUiReady();
            window.bold.lastSyncWalletsReady = instance.isWalletButtonsUiReady();
        } catch (e) {
            console.error('[Bold] payment section sync failed', e);
        } finally {
            window.bold.paymentSyncInProgress = false;
            if (window.bold.pendingPaymentSectionSync) {
                const queued = window.bold.pendingPaymentSectionSync;
                window.bold.pendingPaymentSectionSync = null;
                const walletsReady = !instance.shouldRenderCheckoutWalletButtons()
                    || window.bold.lastSyncWalletsReady;
                if (!queued.hardReload && !queued.refreshWallets
                    && window.bold.lastSyncUiReady && walletsReady) {
                    return;
                }
                window.bold.scheduleBoldPaymentSectionSync(queued);
            }
        }
    };

    window.bold.handleFirecheckoutCheckoutChange = function (sections) {
        if (!window.bold.ensureBoldPaymentRuntime()) {
            return;
        }
        if (sections && sections['shipping-method'] && window.bold.baseInstance) {
            window.bold.baseInstance.invalidateCartDataCache();
        }
        if (!sections) {
            window.bold.scheduleBoldPaymentSectionSync({forceRender: true});
            return;
        }
        if (sections['payment-method']) {
            window.bold.scheduleBoldPaymentSectionSync({hardReload: true});
            return;
        }
        const totalsSections = ['shipping-method', 'total', 'cart'];
        const refreshWallets = totalsSections.some(function (section) {
            return !!sections[section];
        });
        const shouldSync = window.bold.checkoutSectionsTriggeringBoldSync.some(function (section) {
            return !!sections[section];
        });
        if (shouldSync) {
            window.bold.scheduleBoldPaymentSectionSync({
                forceRender: true,
                refreshWallets: refreshWallets
            });
        }
    };

    window.bold.onBoldPaymentFormInjected = function () {
        if (!window.bold.ensureBoldPaymentRuntime()) {
            return;
        }
        // Payment markup injection is followed by firecheckout:paymentMethod:afterInitAfter.
    };

    window.bold.handleFirecheckoutPaymentSectionReload = function () {
        if (!window.bold.ensureBoldPaymentRuntime()) {
            return;
        }
        window.bold.scheduleBoldPaymentSectionSync({hardReload: true});
    };

    window.bold.handleBoldPaymentMethodSelected = function () {
        const instance = window.bold.ensureBoldPaymentRuntime();
        if (!instance) {
            return;
        }
        instance.onBoldPaymentMethodSelected();
    };

    if (!window.bold.firecheckoutPaymentReloadRegistered) {
        window.bold.firecheckoutPaymentReloadRegistered = true;
        document.observe('firecheckout:setResponseAfter', function (e) {
            const sections = e.memo.response && e.memo.response.update_section;
            const baseInstance = window.bold.baseInstance;
            if (window.bold.walletQuoteSyncInProgress
                || (baseInstance && baseInstance.isWalletCheckoutSessionBusy())) {
                if (baseInstance && typeof baseInstance.prefetchCartData === 'function') {
                    baseInstance.prefetchCartData().catch(function (err) {
                        console.warn('[Bold] Cart prefetch during wallet session failed', err);
                    });
                }
                return;
            }
            window.bold.handleFirecheckoutCheckoutChange(sections);
        });
        document.observe('payment-method:switched', function (e) {
            if (e.memo && e.memo.method_code === 'bold') {
                window.bold.handleBoldPaymentMethodSelected();
            }
        });
        document.observe('payment-method:switched-off', function (e) {
            if (e.memo && e.memo.method_code === 'bold' && window.bold.boldPaymentMethod) {
                if (!window.bold.boldPaymentMethod.shouldShowBoldPaymentForm()) {
                    window.bold.boldPaymentMethod.hidePaymentFormWhenNotSelected();
                }
            }
        });
        document.observe('firecheckout:paymentMethod:afterInitAfter', function () {
            const instance = window.bold.boldPaymentMethod;
            if (instance && instance.isPaymentUiReady() && instance.isWalletButtonsUiReady()) {
                instance.ensurePaymentFormVisible();
                instance.ensurePpcpButtonsContainer();
                return;
            }
            window.bold.scheduleBoldPaymentSectionSync({hardReload: true, refreshWallets: true});
        });
        window.bold.registerPaymentMethodDomObserver = function () {
            if (window.bold.paymentMethodDomObserver) {
                return;
            }
            const root = document.getElementById('checkout-payment-method-load')
                || document.getElementById('payment-method');
            if (!root || typeof MutationObserver === 'undefined') {
                return;
            }
            window.bold.paymentMethodDomObserver = new MutationObserver(function (mutations) {
                if (window.bold.spiRenderInProgress) {
                    return;
                }
                if (window.bold.baseInstance && window.bold.baseInstance.isWalletCheckoutSessionBusy()) {
                    return;
                }
                let formInjected = false;
                mutations.forEach(function (mutation) {
                    Array.from(mutation.addedNodes || []).forEach(function (node) {
                        if (node.nodeType !== 1) {
                            return;
                        }
                        if (node.id === 'payment_form_bold'
                            || (node.querySelector && node.querySelector('[id="payment_form_bold"]'))) {
                            formInjected = true;
                        }
                    });
                });
                if (formInjected && !window.bold.paymentSyncInProgress) {
                    window.bold.scheduleBoldPaymentSectionSync({hardReload: true});
                }
            });
            window.bold.paymentMethodDomObserver.observe(root, {childList: true, subtree: true});
        };
        if (document.readyState === 'complete' || document.readyState === 'interactive') {
            window.bold.registerPaymentMethodDomObserver();
        } else {
            document.observe('dom:loaded', window.bold.registerPaymentMethodDomObserver);
        }
    }

    function boldBoosterBootstrap() {
        window.bold.ensureBoldBaseInstance();
        window.bold.initBoldPaymentMethod();
        if (window.bold.boldPaymentMethod
            && typeof window.bold.boldPaymentMethod.registerCheckoutWalletIntentListener === 'function') {
            window.bold.boldPaymentMethod.registerCheckoutWalletIntentListener();
        }
        if (window.bold.baseInstance && typeof window.bold.baseInstance.prefetchCartData === 'function') {
            window.bold.baseInstance.prefetchCartData().catch(function (e) {
                console.warn('[Bold] Initial cart data prefetch failed', e);
            });
        }
        if (typeof window.bold.registerPaymentMethodDomObserver === 'function') {
            window.bold.registerPaymentMethodDomObserver();
        }
        window.bold.scheduleBoldPaymentSectionSync({forceRender: true});
    }

    window.bold.ensureBoldBaseInstance();

    if (!window.bold.firecheckoutCartPrefetchRegistered) {
        window.bold.firecheckoutCartPrefetchRegistered = true;
        document.observe('firecheckout:setResponseAfter', function () {
            if (window.bold.baseInstance && typeof window.bold.baseInstance.prefetchCartData === 'function') {
                window.bold.baseInstance.prefetchCartData().catch(function (e) {
                    console.warn('[Bold] Firecheckout cart data prefetch failed', e);
                });
            }
        });
    }

    if (document.readyState === 'complete' || document.readyState === 'interactive') {
        boldBoosterBootstrap();
    } else {
        document.observe('dom:loaded', boldBoosterBootstrap);
    }
</script>
